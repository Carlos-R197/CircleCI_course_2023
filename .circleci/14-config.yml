version: 2.1

jobs:
    saluda:
        docker:
            - image: ubuntu:latest
        steps:
            - run: echo hola $nombre
            - run: echo Mi id de job es: $CIRCLE_WORKFLOW_JOB_ID

workflows:
    workflow1:
        jobs:
            - saluda
